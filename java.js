$(document).ready(function () {
    $("#time-display").text("current time with moment")
    //other functions function that saves the event to local storage
    // calculate colors for color of schedule block based on current time 
    //

})
